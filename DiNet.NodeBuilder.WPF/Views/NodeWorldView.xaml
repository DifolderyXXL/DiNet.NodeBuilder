<UserControl x:Class="DiNet.NodeBuilder.WPF.Views.NodeWorldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiNet.NodeBuilder.WPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:vm="clr-namespace:DiNet.NodeBuilder.WPF.ViewModels"
             xmlns:v="clr-namespace:DiNet.NodeBuilder.WPF.Views"
             d:DataContext="{d:DesignInstance vm:NodeWorldViewModel}"
             >

    <UserControl.Resources>
        <Style TargetType="v:PortView">
            <EventSetter Event="MouseDown" Handler="Port_MouseDown"/>
            <EventSetter Event="MouseUp" Handler="Port_MouseUp"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="Transparent" PreviewMouseMove="Grid_PreviewMouseMove"  MouseUp="Grid_MouseUp">
        <v:NodeCanvasView x:Name="NodeCanvas"
                          LocalPositionX="{Binding CanvasViewModel.PositionX}"
                          LocalPositionY="{Binding CanvasViewModel.PositionY}"
                          >
            <Grid>
                <ItemsControl ItemsSource="{Binding Nodes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <v:NodeView DataContext="{Binding}" Width="200" VerticalAlignment="Top" HorizontalAlignment="Left" OnHeaderPressed="NodeView_OnHeaderPressed"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                
                <ItemsControl>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </v:NodeCanvasView>
    </Grid>
</UserControl>

<!--
<v:NodeView Width="200" VerticalAlignment="Top" HorizontalAlignment="Left"/>
-->