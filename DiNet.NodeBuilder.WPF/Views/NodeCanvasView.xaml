<UserControl x:Class="DiNet.NodeBuilder.WPF.Views.NodeCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiNet.NodeBuilder.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:vm="clr-namespace:DiNet.NodeBuilder.WPF.ViewModels"
             xmlns:v="clr-namespace:DiNet.NodeBuilder.WPF.Views"
             d:DataContext="{d:DesignInstance vm:NodeCanvasViewModel}"
             >
    <UserControl.Template>
        <ControlTemplate TargetType="UserControl">
            <Grid Background="Transparent" MouseDown="WorldGrid_MouseDown" PreviewMouseMove="Grid_PreviewMouseMove" MouseUp="Grid_MouseUp" PreviewMouseWheel="Grid_PreviewMouseWheel">
                <Grid x:Name="WorldGrid">
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <TranslateTransform X="{Binding LocalPositionX, RelativeSource={RelativeSource AncestorType=local:NodeCanvasView}, Mode=OneWay}" Y="{Binding LocalPositionY, RelativeSource={RelativeSource AncestorType=local:NodeCanvasView}, Mode=OneWay}"/>
                            <ScaleTransform ScaleX="{Binding LocalScale, RelativeSource={RelativeSource AncestorType=local:NodeCanvasView}, Mode=OneWay}" ScaleY="{Binding LocalScale, RelativeSource={RelativeSource AncestorType=local:NodeCanvasView}, Mode=OneWay}"/> 
                        </TransformGroup>
                    </Grid.RenderTransform>
                    <ContentPresenter/>
                </Grid>
            </Grid>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
